# Autonomous Drug Discovery Pipeline — Python Dependencies
#
# Two-step install (conda handles packages that need compiled extensions):
#
#   Step 1 — conda (run once):
#     conda install -n base -c conda-forge rdkit vina openjdk=17 -y
#
#   Step 2 — pip (this file):
#     pip install -r requirements.txt
#
# External binaries (not pip/conda-installable):
#   P2Rank:  wget https://github.com/rdk/p2rank/releases/download/2.5.1/p2rank_2.5.1.tar.gz
#            tar -xzf p2rank_2.5.1.tar.gz  →  expected at ~/p2rank_2.5.1/prank
#   fpocket: git clone https://github.com/Discngine/fpocket && cd fpocket && make install
#            expected at ~/fpocket/bin/fpocket
#
# TargetDiff diffusion model — separate conda env (Python 3.8, PyTorch 1.13):
#   See envs/env_targetdiff.yml

# ── Stage 1: Pocket detection ─────────────────────────────────────────────────
# (P2Rank and fpocket are external binaries; no pip packages needed)

# ── Stage 2: Molecule generation ─────────────────────────────────────────────
# (RDKit installed via conda above; no additional pip packages needed)

# ── Stage 3: Screening ───────────────────────────────────────────────────────
molscore>=0.3          # Primary screening backend (descriptor calc + PAINS filters)
admet-ai>=1.0          # 104-property ADMET prediction (toxicity, absorption, metabolism)

# ── Stage 4: Docking ─────────────────────────────────────────────────────────
meeko>=0.5             # Ligand PDBQT preparation for AutoDock Vina
gemmi>=0.6             # Receptor PDB parsing and AutoDock atom typing
pytdc>=0.4             # TDC Oracle for triage docking mode (Vina under the hood)

# ── Utilities ────────────────────────────────────────────────────────────────
numpy>=1.24            # Numerical arrays (also a transitive dep of rdkit/vina)
pandas>=2.0            # Tabular results handling
scipy>=1.10            # Scientific utilities (geometry, stats)
pyyaml>=6.0            # YAML config generation for TargetDiff sampling
